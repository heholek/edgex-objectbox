// Code generated by ObjectBox; DO NOT EDIT.
// Learn more about defining entities and generating this file - visit https://golang.objectbox.io/entity-annotations

package obx

import (
	"errors"
	"github.com/edgexfoundry/go-mod-core-contracts/models"
	. "github.com/edgexfoundry/go-mod-core-contracts/models"
	"github.com/google/flatbuffers/go"
	"github.com/objectbox/objectbox-go/objectbox"
	"github.com/objectbox/objectbox-go/objectbox/fbutils"
)

type interval_EntityInfo struct {
	objectbox.Entity
	Uid uint64
}

var IntervalBinding = interval_EntityInfo{
	Entity: objectbox.Entity{
		Id: 10,
	},
	Uid: 7776691412575067801,
}

// Interval_ contains type-based Property helpers to facilitate some common operations such as Queries.
var Interval_ = struct {
	Created   *objectbox.PropertyInt64
	Modified  *objectbox.PropertyInt64
	Origin    *objectbox.PropertyInt64
	ID        *objectbox.PropertyUint64
	Name      *objectbox.PropertyString
	Start     *objectbox.PropertyString
	End       *objectbox.PropertyString
	Frequency *objectbox.PropertyString
	Cron      *objectbox.PropertyString
	RunOnce   *objectbox.PropertyBool
}{
	Created: &objectbox.PropertyInt64{
		BaseProperty: &objectbox.BaseProperty{
			Id:     2,
			Entity: &IntervalBinding.Entity,
		},
	},
	Modified: &objectbox.PropertyInt64{
		BaseProperty: &objectbox.BaseProperty{
			Id:     3,
			Entity: &IntervalBinding.Entity,
		},
	},
	Origin: &objectbox.PropertyInt64{
		BaseProperty: &objectbox.BaseProperty{
			Id:     4,
			Entity: &IntervalBinding.Entity,
		},
	},
	ID: &objectbox.PropertyUint64{
		BaseProperty: &objectbox.BaseProperty{
			Id:     1,
			Entity: &IntervalBinding.Entity,
		},
	},
	Name: &objectbox.PropertyString{
		BaseProperty: &objectbox.BaseProperty{
			Id:     5,
			Entity: &IntervalBinding.Entity,
		},
	},
	Start: &objectbox.PropertyString{
		BaseProperty: &objectbox.BaseProperty{
			Id:     6,
			Entity: &IntervalBinding.Entity,
		},
	},
	End: &objectbox.PropertyString{
		BaseProperty: &objectbox.BaseProperty{
			Id:     7,
			Entity: &IntervalBinding.Entity,
		},
	},
	Frequency: &objectbox.PropertyString{
		BaseProperty: &objectbox.BaseProperty{
			Id:     8,
			Entity: &IntervalBinding.Entity,
		},
	},
	Cron: &objectbox.PropertyString{
		BaseProperty: &objectbox.BaseProperty{
			Id:     9,
			Entity: &IntervalBinding.Entity,
		},
	},
	RunOnce: &objectbox.PropertyBool{
		BaseProperty: &objectbox.BaseProperty{
			Id:     10,
			Entity: &IntervalBinding.Entity,
		},
	},
}

// GeneratorVersion is called by ObjectBox to verify the compatibility of the generator used to generate this code
func (interval_EntityInfo) GeneratorVersion() int {
	return 5
}

// AddToModel is called by ObjectBox during model build
func (interval_EntityInfo) AddToModel(model *objectbox.Model) {
	model.Entity("Interval", 10, 7776691412575067801)
	model.Property("Created", 6, 2, 4844875194390890308)
	model.Property("Modified", 6, 3, 7077503010831821587)
	model.Property("Origin", 6, 4, 1376927553496194605)
	model.Property("ID", 6, 1, 6621318798456684065)
	model.PropertyFlags(1)
	model.Property("Name", 9, 5, 5045401852415426063)
	model.PropertyFlags(32)
	model.PropertyIndex(10, 109793853252723826)
	model.Property("Start", 9, 6, 3486349604901008785)
	model.Property("End", 9, 7, 5610259662973560311)
	model.Property("Frequency", 9, 8, 3713977468053692261)
	model.Property("Cron", 9, 9, 3557069308719432604)
	model.Property("RunOnce", 1, 10, 5010559172680658469)
	model.EntityLastPropertyId(10, 5010559172680658469)
}

// GetId is called by ObjectBox during Put operations to check for existing ID on an object
func (interval_EntityInfo) GetId(object interface{}) (uint64, error) {
	if obj, ok := object.(*Interval); ok {
		return objectbox.StringIdConvertToDatabaseValue(obj.ID)
	} else {
		return objectbox.StringIdConvertToDatabaseValue(object.(Interval).ID)
	}
}

// SetId is called by ObjectBox during Put to update an ID on an object that has just been inserted
func (interval_EntityInfo) SetId(object interface{}, id uint64) error {
	if obj, ok := object.(*Interval); ok {
		var err error
		obj.ID, err = objectbox.StringIdConvertToEntityProperty(id)
		return err
	} else {
		// NOTE while this can't update, it will at least behave consistently (panic in case of a wrong type)
		_ = object.(Interval).ID
		return nil
	}
}

// PutRelated is called by ObjectBox to put related entities before the object itself is flattened and put
func (interval_EntityInfo) PutRelated(ob *objectbox.ObjectBox, object interface{}, id uint64) error {
	return nil
}

// Flatten is called by ObjectBox to transform an object to a FlatBuffer
func (interval_EntityInfo) Flatten(object interface{}, fbb *flatbuffers.Builder, id uint64) error {
	var obj *Interval
	if objPtr, ok := object.(*Interval); ok {
		obj = objPtr
	} else {
		objVal := object.(Interval)
		obj = &objVal
	}

	var offsetName = fbutils.CreateStringOffset(fbb, obj.Name)
	var offsetStart = fbutils.CreateStringOffset(fbb, obj.Start)
	var offsetEnd = fbutils.CreateStringOffset(fbb, obj.End)
	var offsetFrequency = fbutils.CreateStringOffset(fbb, obj.Frequency)
	var offsetCron = fbutils.CreateStringOffset(fbb, obj.Cron)

	// build the FlatBuffers object
	fbb.StartObject(10)
	fbutils.SetInt64Slot(fbb, 1, obj.Timestamps.Created)
	fbutils.SetInt64Slot(fbb, 2, obj.Timestamps.Modified)
	fbutils.SetInt64Slot(fbb, 3, obj.Timestamps.Origin)
	fbutils.SetUint64Slot(fbb, 0, id)
	fbutils.SetUOffsetTSlot(fbb, 4, offsetName)
	fbutils.SetUOffsetTSlot(fbb, 5, offsetStart)
	fbutils.SetUOffsetTSlot(fbb, 6, offsetEnd)
	fbutils.SetUOffsetTSlot(fbb, 7, offsetFrequency)
	fbutils.SetUOffsetTSlot(fbb, 8, offsetCron)
	fbutils.SetBoolSlot(fbb, 9, obj.RunOnce)
	return nil
}

// Load is called by ObjectBox to load an object from a FlatBuffer
func (interval_EntityInfo) Load(ob *objectbox.ObjectBox, bytes []byte) (interface{}, error) {
	if len(bytes) == 0 { // sanity check, should "never" happen
		return nil, errors.New("can't deserialize an object of type 'Interval' - no data received")
	}

	var table = &flatbuffers.Table{
		Bytes: bytes,
		Pos:   flatbuffers.GetUOffsetT(bytes),
	}

	propID, err := objectbox.StringIdConvertToEntityProperty(fbutils.GetUint64Slot(table, 4))
	if err != nil {
		return nil, errors.New("converter objectbox.StringIdConvertToEntityProperty() failed on Interval.ID: " + err.Error())
	}

	return &Interval{
		Timestamps: models.Timestamps{
			Created:  fbutils.GetInt64Slot(table, 6),
			Modified: fbutils.GetInt64Slot(table, 8),
			Origin:   fbutils.GetInt64Slot(table, 10),
		},
		ID:        propID,
		Name:      fbutils.GetStringSlot(table, 12),
		Start:     fbutils.GetStringSlot(table, 14),
		End:       fbutils.GetStringSlot(table, 16),
		Frequency: fbutils.GetStringSlot(table, 18),
		Cron:      fbutils.GetStringSlot(table, 20),
		RunOnce:   fbutils.GetBoolSlot(table, 22),
	}, nil
}

// MakeSlice is called by ObjectBox to construct a new slice to hold the read objects
func (interval_EntityInfo) MakeSlice(capacity int) interface{} {
	return make([]Interval, 0, capacity)
}

// AppendToSlice is called by ObjectBox to fill the slice of the read objects
func (interval_EntityInfo) AppendToSlice(slice interface{}, object interface{}) interface{} {
	if object == nil {
		return append(slice.([]Interval), Interval{})
	}
	return append(slice.([]Interval), *object.(*Interval))
}

// Box provides CRUD access to Interval objects
type IntervalBox struct {
	*objectbox.Box
}

// BoxForInterval opens a box of Interval objects
func BoxForInterval(ob *objectbox.ObjectBox) *IntervalBox {
	return &IntervalBox{
		Box: ob.InternalBox(10),
	}
}

// Put synchronously inserts/updates a single object.
// In case the ID is not specified, it would be assigned automatically (auto-increment).
// When inserting, the Interval.ID property on the passed object will be assigned the new ID as well.
func (box *IntervalBox) Put(object *Interval) (uint64, error) {
	return box.Box.Put(object)
}

// Insert synchronously inserts a single object. As opposed to Put, Insert will fail if given an ID that already exists.
// In case the ID is not specified, it would be assigned automatically (auto-increment).
// When inserting, the Interval.ID property on the passed object will be assigned the new ID as well.
func (box *IntervalBox) Insert(object *Interval) (uint64, error) {
	return box.Box.Insert(object)
}

// Update synchronously updates a single object.
// As opposed to Put, Update will fail if an object with the same ID is not found in the database.
func (box *IntervalBox) Update(object *Interval) error {
	return box.Box.Update(object)
}

// PutAsync asynchronously inserts/updates a single object.
// Deprecated: use box.Async().Put() instead
func (box *IntervalBox) PutAsync(object *Interval) (uint64, error) {
	return box.Box.PutAsync(object)
}

// PutMany inserts multiple objects in single transaction.
// In case IDs are not set on the objects, they would be assigned automatically (auto-increment).
//
// Returns: IDs of the put objects (in the same order).
// When inserting, the Interval.ID property on the objects in the slice will be assigned the new IDs as well.
//
// Note: In case an error occurs during the transaction, some of the objects may already have the Interval.ID assigned
// even though the transaction has been rolled back and the objects are not stored under those IDs.
//
// Note: The slice may be empty or even nil; in both cases, an empty IDs slice and no error is returned.
func (box *IntervalBox) PutMany(objects []Interval) ([]uint64, error) {
	return box.Box.PutMany(objects)
}

// Get reads a single object.
//
// Returns nil (and no error) in case the object with the given ID doesn't exist.
func (box *IntervalBox) Get(id uint64) (*Interval, error) {
	object, err := box.Box.Get(id)
	if err != nil {
		return nil, err
	} else if object == nil {
		return nil, nil
	}
	return object.(*Interval), nil
}

// GetMany reads multiple objects at once.
// If any of the objects doesn't exist, its position in the return slice is an empty object
func (box *IntervalBox) GetMany(ids ...uint64) ([]Interval, error) {
	objects, err := box.Box.GetMany(ids...)
	if err != nil {
		return nil, err
	}
	return objects.([]Interval), nil
}

// GetManyExisting reads multiple objects at once, skipping those that do not exist.
func (box *IntervalBox) GetManyExisting(ids ...uint64) ([]Interval, error) {
	objects, err := box.Box.GetManyExisting(ids...)
	if err != nil {
		return nil, err
	}
	return objects.([]Interval), nil
}

// GetAll reads all stored objects
func (box *IntervalBox) GetAll() ([]Interval, error) {
	objects, err := box.Box.GetAll()
	if err != nil {
		return nil, err
	}
	return objects.([]Interval), nil
}

// Remove deletes a single object
func (box *IntervalBox) Remove(object *Interval) error {
	return box.Box.Remove(object)
}

// RemoveMany deletes multiple objects at once.
// Returns the number of deleted object or error on failure.
// Note that this method will not fail if an object is not found (e.g. already removed).
// In case you need to strictly check whether all of the objects exist before removing them,
// you can execute multiple box.Contains() and box.Remove() inside a single write transaction.
func (box *IntervalBox) RemoveMany(objects ...*Interval) (uint64, error) {
	var ids = make([]uint64, len(objects))
	var err error
	for k, object := range objects {
		ids[k], err = objectbox.StringIdConvertToDatabaseValue(object.ID)
		if err != nil {
			return 0, errors.New("converter objectbox.StringIdConvertToDatabaseValue() failed on Interval.ID: " + err.Error())
		}
	}
	return box.Box.RemoveIds(ids...)
}

// Creates a query with the given conditions. Use the fields of the Interval_ struct to create conditions.
// Keep the *IntervalQuery if you intend to execute the query multiple times.
// Note: this function panics if you try to create illegal queries; e.g. use properties of an alien type.
// This is typically a programming error. Use QueryOrError instead if you want the explicit error check.
func (box *IntervalBox) Query(conditions ...objectbox.Condition) *IntervalQuery {
	return &IntervalQuery{
		box.Box.Query(conditions...),
	}
}

// Creates a query with the given conditions. Use the fields of the Interval_ struct to create conditions.
// Keep the *IntervalQuery if you intend to execute the query multiple times.
func (box *IntervalBox) QueryOrError(conditions ...objectbox.Condition) (*IntervalQuery, error) {
	if query, err := box.Box.QueryOrError(conditions...); err != nil {
		return nil, err
	} else {
		return &IntervalQuery{query}, nil
	}
}

// Async provides access to the default Async Box for asynchronous operations. See IntervalAsyncBox for more information.
func (box *IntervalBox) Async() *IntervalAsyncBox {
	return &IntervalAsyncBox{AsyncBox: box.Box.Async()}
}

// IntervalAsyncBox provides asynchronous operations on Interval objects.
//
// Asynchronous operations are executed on a separate internal thread for better performance.
//
// There are two main use cases:
//
// 1) "execute & forget:" you gain faster put/remove operations as you don't have to wait for the transaction to finish.
//
// 2) Many small transactions: if your write load is typically a lot of individual puts that happen in parallel,
// this will merge small transactions into bigger ones. This results in a significant gain in overall throughput.
//
// In situations with (extremely) high async load, an async method may be throttled (~1ms) or delayed up to 1 second.
// In the unlikely event that the object could still not be enqueued (full queue), an error will be returned.
//
// Note that async methods do not give you hard durability guarantees like the synchronous Box provides.
// There is a small time window in which the data may not have been committed durably yet.
type IntervalAsyncBox struct {
	*objectbox.AsyncBox
}

// AsyncBoxForInterval creates a new async box with the given operation timeout in case an async queue is full.
// The returned struct must be freed explicitly using the Close() method.
// It's usually preferable to use IntervalBox::Async() which takes care of resource management and doesn't require closing.
func AsyncBoxForInterval(ob *objectbox.ObjectBox, timeoutMs uint64) *IntervalAsyncBox {
	var async, err = objectbox.NewAsyncBox(ob, 10, timeoutMs)
	if err != nil {
		panic("Could not create async box for entity ID 10: %s" + err.Error())
	}
	return &IntervalAsyncBox{AsyncBox: async}
}

// Put inserts/updates a single object asynchronously.
// When inserting a new object, the ID property on the passed object will be assigned the new ID the entity would hold
// if the insert is ultimately successful. The newly assigned ID may not become valid if the insert fails.
func (asyncBox *IntervalAsyncBox) Put(object *Interval) (uint64, error) {
	return asyncBox.AsyncBox.Put(object)
}

// Insert a single object asynchronously.
// The ID property on the passed object will be assigned the new ID the entity would hold if the insert is ultimately
// successful. The newly assigned ID may not become valid if the insert fails.
// Fails silently if an object with the same ID already exists (this error is not returned).
func (asyncBox *IntervalAsyncBox) Insert(object *Interval) (id uint64, err error) {
	return asyncBox.AsyncBox.Insert(object)
}

// Update a single object asynchronously.
// The object must already exists or the update fails silently (without an error returned).
func (asyncBox *IntervalAsyncBox) Update(object *Interval) error {
	return asyncBox.AsyncBox.Update(object)
}

// Remove deletes a single object asynchronously.
func (asyncBox *IntervalAsyncBox) Remove(object *Interval) error {
	return asyncBox.AsyncBox.Remove(object)
}

// Query provides a way to search stored objects
//
// For example, you can find all Interval which ID is either 42 or 47:
// 		box.Query(Interval_.ID.In(42, 47)).Find()
type IntervalQuery struct {
	*objectbox.Query
}

// Find returns all objects matching the query
func (query *IntervalQuery) Find() ([]Interval, error) {
	objects, err := query.Query.Find()
	if err != nil {
		return nil, err
	}
	return objects.([]Interval), nil
}

// Offset defines the index of the first object to process (how many objects to skip)
func (query *IntervalQuery) Offset(offset uint64) *IntervalQuery {
	query.Query.Offset(offset)
	return query
}

// Limit sets the number of elements to process by the query
func (query *IntervalQuery) Limit(limit uint64) *IntervalQuery {
	query.Query.Limit(limit)
	return query
}
